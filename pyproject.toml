[project]
name = "dotfiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.15.13",
]

[project.scripts]
dotfiles = "installer.main:main"

[tool.uv]
package = true
